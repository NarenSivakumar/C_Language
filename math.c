#include<stdio.h>
#include<math.h>

int main()
{

    double a = sqrt(4);
    double b = pow(2,5);
    int c = round(3.14);
    int d = ceil(3.14);
    int e = floor(3.14);
    double f = log(3);
    double g = sin(45);
    printf("%lf %lf %lf %lf %d %d %d\n",a,b,f,g,c,d,e);
}